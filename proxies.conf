# Trusted proxy IPs/CIDRs (one per line)
# The backend will use X-Forwarded-For / X-Real-IP from these sources
# to determine the real client IP.
#
# Private/Docker network ranges (10.x, 172.16-31.x, 192.168.x) are
# trusted automatically — no need to add them here.
#
# Only add PUBLIC proxy IPs if you have an external (non-Docker) reverse proxy:
#   203.0.113.50
#   198.51.100.0/24
